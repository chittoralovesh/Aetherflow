- name: default
  kind: postgres
  configuration:
    connection_info:
      database_url:
        from_env: NHOST_DATABASE_URL
      pool_settings:
        max_connections: 50
        idle_timeout: 180
        retries: 1
  tables: "!include default/tables/tables.yaml"
